/** Hard cap for how far ahead customers can book (UI + API normalization). */
export const MAX_BOOKING_ADVANCE_DAYS = 1825;

/** Default when a business has no rule yet (matches backend default for new businesses). */
export const DEFAULT_MAX_BOOKING_ADVANCE_DAYS = 730;
