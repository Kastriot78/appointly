# Origin only — no trailing slash, no `/api` (paths in code already include `/api/...`).
VITE_API_URL=http://localhost:5000

# Optional: public marketing / booking site origin for “Book with me” share links.
# Use when the dashboard runs on another domain (no trailing slash).
# VITE_PUBLIC_APP_URL=https://appointments.example.com
